<Window x:Class="Procmon.Views.PerformanceChartWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Performance Charts" Height="700" Width="1200" MinHeight="500" MinWidth="800"
        Background="#FF1E1E1E" WindowStyle="SingleBorderWindow">
    
    <Window.Resources>
        <!-- Modern Color Palette -->
        <SolidColorBrush x:Key="DarkBackground" Color="#FF1E1E1E"/>
        <SolidColorBrush x:Key="MediumBackground" Color="#FF2D2D30"/>
        <SolidColorBrush x:Key="LightBackground" Color="#FF3F3F46"/>
        <SolidColorBrush x:Key="AccentBlue" Color="#FF007ACC"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#FFFFFFFF"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#FFB0B0B0"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#FF464649"/>

        <!-- Modern Button Style -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Color="#FF0078D4" Offset="0"/>
                        <GradientStop Color="#FF005A9E" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                CornerRadius="4" BorderThickness="0">
                            <Border.Effect>
                                <DropShadowEffect Color="Black" Direction="270" ShadowDepth="1" 
                                                BlurRadius="4" Opacity="0.3"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                            <GradientStop Color="#FF106EBE" Offset="0"/>
                                            <GradientStop Color="#FF005A9E" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                            <GradientStop Color="#FF005A9E" Offset="0"/>
                                            <GradientStop Color="#FF004578" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern CheckBox Style -->
        <Style x:Key="ModernCheckBox" TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="FontWeight" Value="Medium"/>
        </Style>
    </Window.Resources>
    
    <Grid Background="{StaticResource DarkBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Control Panel -->
        <Border Grid.Row="0" Background="{StaticResource MediumBackground}" 
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
            <Border.Effect>
                <DropShadowEffect Color="Black" Direction="270" ShadowDepth="1" 
                                BlurRadius="4" Opacity="0.2"/>
            </Border.Effect>
            <StackPanel Orientation="Horizontal" Margin="16,12">
                <TextBlock Text="Show Data:" VerticalAlignment="Center" Foreground="{StaticResource TextPrimary}" 
                           FontWeight="SemiBold" FontSize="13" Margin="0,0,16,0"/>
                <CheckBox Name="ShowCpuCheckBox" Content="CPU" IsChecked="True" Style="{StaticResource ModernCheckBox}"
                          Checked="UpdateChart" Unchecked="UpdateChart"/>
                <CheckBox Name="ShowRamCheckBox" Content="RAM" IsChecked="True" Style="{StaticResource ModernCheckBox}"
                          Checked="UpdateChart" Unchecked="UpdateChart"/>
                <CheckBox Name="ShowGpuCoreCheckBox" Content="GPU Core" IsChecked="True" Style="{StaticResource ModernCheckBox}"
                          Checked="UpdateChart" Unchecked="UpdateChart"/>
                <CheckBox Name="ShowGpuVideoCheckBox" Content="GPU Video" IsChecked="True" Style="{StaticResource ModernCheckBox}"
                          Checked="UpdateChart" Unchecked="UpdateChart"/>
                <CheckBox Name="ShowGpuVramCheckBox" Content="GPU VRAM" IsChecked="True" Style="{StaticResource ModernCheckBox}"
                          Checked="UpdateChart" Unchecked="UpdateChart"/>
                <Button Name="ClearChartButton" Content="Clear Chart" Style="{StaticResource ModernButton}" 
                        Click="ClearChartButton_Click" Margin="24,0,0,0"/>
            </StackPanel>
        </Border>
        
        <!-- Chart Area -->
        <Grid Grid.Row="1" Margin="16">
            <Border Background="{StaticResource MediumBackground}" CornerRadius="8" 
                    BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                <Border.Effect>
                    <DropShadowEffect Color="Black" Direction="270" ShadowDepth="2" 
                                    BlurRadius="8" Opacity="0.3"/>
                </Border.Effect>
                <Grid Margin="8">
                    <Canvas Name="ChartCanvas" Background="#FF252526" ClipToBounds="True">
                        <Canvas.RenderTransform>
                            <ScaleTransform x:Name="ChartScaleTransform"/>
                        </Canvas.RenderTransform>
                    </Canvas>
                    
                    <!-- Legend -->
                    <Border HorizontalAlignment="Right" VerticalAlignment="Top" 
                            Background="{StaticResource LightBackground}" 
                            BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                            CornerRadius="6" Margin="16" Padding="12,8">
                        <Border.Effect>
                            <DropShadowEffect Color="Black" Direction="270" ShadowDepth="1" 
                                            BlurRadius="4" Opacity="0.3"/>
                        </Border.Effect>
                        <StackPanel Orientation="Vertical">
                            <TextBlock Text="LEGEND" FontWeight="Bold" FontSize="11" 
                                       Foreground="{StaticResource TextSecondary}" Margin="0,0,0,6"/>
                            <TextBlock Name="CpuLegend" Text="- CPU Load (%)" Foreground="#FFE74C3C" 
                                       Margin="0,2" FontWeight="Medium" FontSize="11"/>
                            <TextBlock Name="RamLegend" Text="- RAM Usage (%)" Foreground="#FF3498DB" 
                                       Margin="0,2" FontWeight="Medium" FontSize="11"/>
                            <TextBlock Name="GpuCoreLegend" Text="- GPU Core (%)" Foreground="#FF2ECC71" 
                                       Margin="0,2" FontWeight="Medium" FontSize="11"/>
                            <TextBlock Name="GpuVideoLegend" Text="- GPU Video (%)" Foreground="#FFF39C12" 
                                       Margin="0,2" FontWeight="Medium" FontSize="11"/>
                            <TextBlock Name="GpuVramLegend" Text="- GPU VRAM (%)" Foreground="#FF9B59B6" 
                                       Margin="0,2" FontWeight="Medium" FontSize="11"/>
                        </StackPanel>
                    </Border>
                    
                    <!-- Y-Axis Labels -->
                    <StackPanel Name="YAxisLabels" Orientation="Vertical" HorizontalAlignment="Left" 
                                VerticalAlignment="Stretch" Width="40" 
                                Background="{StaticResource LightBackground}" Opacity="0.9"/>
                    
                    <!-- X-Axis Labels -->
                    <StackPanel Name="XAxisLabels" Orientation="Horizontal" HorizontalAlignment="Stretch" 
                                VerticalAlignment="Bottom" Height="25" 
                                Background="{StaticResource LightBackground}" Opacity="0.9"/>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>